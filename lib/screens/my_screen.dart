import 'package:flutter/material.dart';

class MyScreeen extends StatelessWidget {
  const MyScreeen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("dsdsds"),
      ),
    );
  }
}
